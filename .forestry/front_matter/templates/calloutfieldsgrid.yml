---
label: CalloutFieldsGrid
hide_body: false
fields:
- name: callouts
  type: list
  config:
    use_select: true
    min: 1
    max: 
    source:
      type: pages
      section: callouts
  label: Grid Item
